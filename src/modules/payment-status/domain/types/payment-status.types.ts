/**
 * Data required to create a new payment status (excluding computed properties and auto-generated fields)
 */
export type CreatePaymentStatusData = {
    name: string;
    description?: string;
};
